apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: data-collector

# Note: 11-secret.example.yaml is a template — copy it to 11-secret.yaml,
# fill in, and add it here (it is intentionally not listed so the example is
# never applied as-is).
resources:
  - 00-namespace.yaml
  - 10-configmap.yaml
  - 20-pv-pvc.yaml
  - 30-deployment.yaml
  - 40-service.yaml
  - 50-ingress.yaml
  - 60-hpa.yaml
  - 70-pdb.yaml

images:
  - name: ghcr.io/duosis-developer-team/data-collector-webservice
    newTag: "0.1.0"
